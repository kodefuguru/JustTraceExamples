<UserControl x:Class="JustTraceExamples.TimerLeak"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             Background="Black"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{StaticResource DarkCheckered}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding DisplayTime}" Style="{StaticResource TimeTextStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.Row="1">

            <StackPanel Orientation="Horizontal"
					HorizontalAlignment="Center"
					Margin="0,5,0,0"
                    >
                <telerik:RadButton Width="150"
                                   Height="32"
							   Content="Close"
							   Margin="10 10 10 20"
							   Click="Close"
                               Style="{StaticResource ButtonTextStyle}">
                </telerik:RadButton>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
